# Example environment variables for OTP delivery providers

# ------------------
# SendGrid (email)
# ------------------
# SENDGRID_API_KEY (required if you want to use SendGrid)
# FROM_EMAIL should be a verified sender in your SendGrid account
SENDGRID_API_KEY=SG.xxxxxxxx-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
FROM_EMAIL=no-reply@example.com

# ------------------
# SMTP (email) - e.g. Mailtrap or any SMTP provider
# ------------------
# If using Mailtrap, use the host/user/pass from the inbox
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_pass
# FROM_EMAIL can be same as above or different
# FROM_EMAIL=no-reply@example.com

# ------------------
# Twilio (SMS)
# ------------------
# TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN, TWILIO_FROM (E.164 phone number)
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_FROM=+12345678901

# ------------------
# Dev helpers
# ------------------
# When true the API will include additional debugging info (otp on signup and delivery diagnostics)
DEV_INCLUDE_OTP=true

# PORT the server listens on (default 5001)
PORT=5001
